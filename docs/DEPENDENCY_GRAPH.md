# 依赖关系图与实施排期

## Wave 0: 并行起步（无前置依赖，可立即开始）

```
#24 Crate 重组 ──────────────────────────────────────┐
#10 ApiKeyInfo scope bug（~2h）                       │
#11 OEN 双 Envelope 统一（~4h）                        ├─→ 阻塞所有后续 Wave
#12 OenVerifier 签名缓存修复（~2h）                     │
#13 租户隔离强制（~4h）                                 │
#15 PoisonGuard 新模块（独立）                         ├──→ 阻塞 #9 #27 #33
#20 可插拔引擎 Trait（接口定义）                       ├──→ 阻塞 #31 #32
```

**策略**：4 个 bug 修复可立即并行（改动小、风险低），#24 和 #15/#20 同步启动。

---

## Wave 1: 数据层与身份解析（依赖 Wave 0 #24）

```
#24 ──→ #1 PostgreSQL+pgvector ──┬──→ Wave 2
        #2 Redis ────────────────┤
        #25 Identity Resolver ────┤──→ 阻塞 #6 #7 #26 #35
                                 │
                                 └──→ Wave 2
```

**关键路径**：#1 和 #2 可并行；#25 依赖 #2 完成后启动。

---

## Wave 2: 核心存储与权限（依赖 Wave 1 #1 #2）

```
#1 ──→ #8 合并存储 ──────────────┬──→ #9 #16 #28
      #3 Canonical User ────────┤──→ #7 #35
      #5 Shared Task ────────────┤──→ #27 #29 #35
      #14 RBAC+ABAC+RLS ────────┤──→ #26 #34 #18 #32 #23 #22
      #4 pgvector HNSW ────────┤──→ #18 #34
#1+#2 → #17 Outbox ─────────────┤──→ #9(异步写入)
#8 ──→ #16 Audit/Version ──────┤──→ #28 #22
```

**7 个 issue 可大量并行**，是最高吞吐量的 Wave。

---

## Wave 3: 控制面逻辑（依赖 Wave 2）

```
#25+#13 → #26 API Gateway ───────────→ #35
#25+#2  → #6 Context Router ─────────→ #7
#8+#15+#17 → #9 Write Pipeline ─────→ #21
#5 → #29 Decision Record ───────────→ #21
#1 → #30 Entity ────────────────────→ #23
#8+#16 → #28 Conflict Resolution ───→ #18
```

---

## Wave 4: 装配与检索（依赖 Wave 3）

```
#6+#3 → #7 Context Assembler ──────→ #27 #35
#5+#7+#15 → #27 Context Package ──→ #35
#4+#28+#14 → #18 混合召回+Rerank ─→ #19
#4+#14 → #34 有界检索预过滤
#20 → #31 超时熔断 ──────────────→ #32
```

---

## Wave 5: 韧性、安全与集成（依赖 Wave 4）

```
#31+#14 → #32 降级策略 ──────────→ #35
#15 → #33 数据加密
（大多数模块就绪）→ #19 SLO 监控
#3+#5+#25+#26+#7+#27 → #35 适配器
```

---

## Wave 6: 学习闭环（依赖 Wave 5）

```
#9+#29 → #21 Reflection & Consolidation
#14+#16 → #22 Scope 晋级
#30+#14 → #23 SoT 回查
```

---

## 关键路径（最长依赖链）

```
#24 → #1 → #8 → #9 → #21
      → #2 → #25 → #6 → #7 → #27 → #35
      → #1 → #14 → #18 → #19
```

最深处 7 级，对应文档 §13 的阶段 0-2（约 24 周）。
