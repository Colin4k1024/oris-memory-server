//! Memory domain types — re-exported from oris-memory-contract.
//!
//! These types were originally defined here; they have been promoted to the
//! contract crate so that all workspace members can share them without
//! circular dependencies.

pub use oris_memory_contract::memory_types::*;
